let express;_604‍.w("express",[["default",["express"],function(v){express=v}]]);// import { ApolloServer } from "apollo-server-express";


(() => {
  const app = express()

  const PORT = process.env.SERVER_PORT || 4000
  const HOST = process.env.SERVER_HOST || 'http://localhost'
  try {
    app.listen({ PORT }, () => {
      console.log(`🚀  Server ready at ${HOST}:${PORT} `)
    })
  } catch (err) {
    console.error(`
 server error: ${err}
 `)
  }
})()